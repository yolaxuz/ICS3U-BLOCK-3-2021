/*public class Die {
    // attributes - defines the state of the object 

    // The attributes shoule be private 
    // private -> visible in this class 
    private int numSide; 
    private int topSide; 

    //constructor
    // has the same name as the class
    // you should create at leas 1
    // they are used to obtain (contruct an instance of this class)
    //in this case it will give a die 
    // purpose is to initialise the state (attributes) 
    public Die(){
        numSides = 6; 
        topSide = (int)(Mat.random()*numSides) + 1;  
    }

}
*/